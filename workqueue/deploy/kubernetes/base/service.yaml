apiVersion: v1
kind: Service
metadata:
  name: factory-admin
  labels:
    app.kubernetes.io/name: factory-admin
    app.kubernetes.io/part-of: factory
spec:
  selector:
    app.kubernetes.io/name: factory-admin
  ports:
    - name: http
      port: 8080
      targetPort: http
