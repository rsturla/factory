apiVersion: apps/v1
kind: Deployment
metadata:
  name: factory-receiver
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: factory-receiver
  template:
    metadata:
      labels:
        app.kubernetes.io/name: factory-receiver
        app.kubernetes.io/component: receiver
    spec:
      containers:
        - name: receiver
          image: quay.io/hummingbird/factory-receiver
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: PG_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: factory-db
                  key: uri
            - name: FACTORY_LISTEN_ADDR
              value: ":8080"
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: factory-receiver
spec:
  selector:
    app.kubernetes.io/name: factory-receiver
  ports:
    - name: http
      port: 8080
      targetPort: http
