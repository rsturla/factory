apiVersion: v1
kind: Secret
metadata:
  name: vulndb
type: Opaque
stringData:
  uri: postgresql://vulndb:vulndb@vulndb:5432/vulndb?sslmode=disable
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vulndb
  labels:
    app.kubernetes.io/name: vulndb
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: vuln-ingest
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
---
apiVersion: v1
kind: Service
metadata:
  name: vulndb
  labels:
    app.kubernetes.io/name: vulndb
    app.kubernetes.io/part-of: vuln-ingest
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: vulndb
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: vulndb
  labels:
    app.kubernetes.io/name: vulndb
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: vuln-ingest
spec:
  serviceName: vulndb
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: vulndb
  template:
    metadata:
      labels:
        app.kubernetes.io/name: vulndb
        app.kubernetes.io/component: database
        app.kubernetes.io/part-of: vuln-ingest
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: postgres
          image: quay.io/hummingbird/postgresql:18
          ports:
            - name: postgres
              containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: vulndb
            - name: POSTGRES_USER
              value: vulndb
            - name: POSTGRES_PASSWORD
              value: vulndb
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql
            - name: run
              mountPath: /var/run/postgresql
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "vulndb"]
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "vulndb"]
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: vulndb
        - name: run
          emptyDir: {}
