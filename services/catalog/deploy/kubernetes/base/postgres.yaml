apiVersion: v1
kind: Secret
metadata:
  name: catalogdb
type: Opaque
stringData:
  uri: postgresql://catalogdb:catalogdb@catalogdb:5432/catalogdb?sslmode=disable
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: catalogdb
  labels:
    app.kubernetes.io/name: catalogdb
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: catalog
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
---
apiVersion: v1
kind: Service
metadata:
  name: catalogdb
  labels:
    app.kubernetes.io/name: catalogdb
    app.kubernetes.io/part-of: catalog
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: catalogdb
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: catalogdb
  labels:
    app.kubernetes.io/name: catalogdb
    app.kubernetes.io/component: database
    app.kubernetes.io/part-of: catalog
spec:
  serviceName: catalogdb
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: catalogdb
  template:
    metadata:
      labels:
        app.kubernetes.io/name: catalogdb
        app.kubernetes.io/component: database
        app.kubernetes.io/part-of: catalog
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: postgres
          image: quay.io/hummingbird/postgresql:18
          ports:
            - name: postgres
              containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: catalogdb
            - name: POSTGRES_USER
              value: catalogdb
            - name: POSTGRES_PASSWORD
              value: catalogdb
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
            - name: POSTGRES_MAX_CONNECTIONS
              value: "300"
            - name: POSTGRES_SHARED_BUFFERS
              value: 512MB
            - name: POSTGRES_WORK_MEM
              value: 16MB
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: [ALL]
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql
            - name: run
              mountPath: /var/run/postgresql
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "catalogdb"]
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "catalogdb"]
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: catalogdb
        - name: run
          emptyDir: {}
